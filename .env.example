# Toggle between dummy data (true) and real backend API (false)
VITE_USE_DUMMY_API=true

# Base URL for the real backend API (used only when VITE_USE_DUMMY_API=false)
VITE_API_BASE_URL=https://api.example.com
